import React from 'react';
import ListCourse from '../../components/ListCourse';

const Course = () => {
    return (
        <div>
            <ListCourse />
        </div>
    );
};

export default Course;
